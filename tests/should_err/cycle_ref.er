# ERR: a mutable object must not contain itself
a as List!(Obj, _) = ![]
a.push! a

# ERR: two mutable objects that contain each other
b as List!(Obj, _) = ![]
c as List!(Obj, _) = ![]
b.push! c
# ERR: pushing `c` into `b` moved it, so this is also a use of a moved variable
c.push! b
